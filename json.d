module dalet.json;

import std.stdio;
import std.file;

void parseJson( string filename ) {
	string jsonContent = readText( filename );
	/* parse json
	 * turn into struct
	 * write to file_name.d
	 * (add file_name module)
	 */
}