import dalet.math;

import std.stdio;

int main() {
	// testing out the all functions
	int num1 = fact( 5 );
	float num2 = pow( 15.0, 2 );
	float num3 = sin( 80 );
	int num4 = abs( 0 ); 
	writeln( num1 );

	return 0;
}